describe("Update", () => {
	it('should return an updated {{{props.model_name_sLc}}} (HttpClient called once)', (done: DoneFn) => {

		httpClientSpy.patch.and.returnValue(defer(() => Promise.resolve(mock{{{props.model_name_sUcfirst}}})));

		{{{props.route_name_pLf}}}Service
			.update({{#if props.isPrimaryKeyNumber}}1{{else}}'1'{{/if}},mockUpdate{{{props.model_name_sUcfirst}}})
			.then(({{{props.model_name_sLc}}}) => {
				expect({{{props.model_name_sLc}}}).withContext('expected {{{props.model_name_sLc}}}').toEqual(mock{{{props.model_name_sUcfirst}}});
				done();
			})
			.catch((reason) => done.fail('Request should not fail !'));
		expect(httpClientSpy.get.calls.count()).withContext('one call').toBe(1);
	});
});
