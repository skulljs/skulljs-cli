describe('Remove', () => {
    it('should return a deleted {{{ props.model_name_sUcfirst }}}', async () => {
      const removeSpy = jest.spyOn(service, 'remove');
      expect(controller.remove(1)).resolves.toBe(mock{{{ props.model_name_sUcfirst }}});
      expect(removeSpy).toHaveBeenCalledTimes(1);
      expect(removeSpy).toHaveBeenCalledWith(1);
    });
});