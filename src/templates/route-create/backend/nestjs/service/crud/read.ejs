  findAll() {
    return this.prisma.<%= props.model_name_Lc %>.findMany();
  }

  findOne(<%= props.model_id %>: <%= props.model_id_type %>) {
    return this.prisma.<%= props.model_name_Lc %>.findUnique({ where: { <%= props.model_id %>} });
  }
