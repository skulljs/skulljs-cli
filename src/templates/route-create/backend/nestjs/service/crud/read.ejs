  async findAll() {
    return prisma.<%= props.model_name_pLc %>.findMany();
  }

  findOne(<%= props.model_id %>: <%= props.model_id_type %>) {
    return prisma.<%= props.model_name_pLc %>.findUnique({ where: { <%= props.model_id %> } });
  }
