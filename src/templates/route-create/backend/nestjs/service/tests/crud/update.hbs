describe('Update', () => {
	it('should return an updated {{{props.model_name_sUcfirst}}}', async () => {
		const updateSpy = jest.spyOn(prisma.{{{props.model_name_pLc}}}, 'update');
		expect(service.update(1, mockUpdate{{{props.model_name_sUcfirst}}})).resolves.toBe(mock{{{props.model_name_sUcfirst}}});
		expect(updateSpy).toHaveBeenCalledTimes(1);
		expect(updateSpy).toHaveBeenCalledWith({ where: { id: 1 }, data: mockUpdate{{{props.model_name_sUcfirst}}} });
	});
});