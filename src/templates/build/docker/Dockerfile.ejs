FROM node:lts

# Create app directory
WORKDIR /app

# Install app dependencies
COPY package*.json ./
RUN npm install

# Copy app source
COPY . .
<% props.dockerfile_opt_runs.forEach(function(run) { %>
<%= run %><% }); %>

EXPOSE <%= props.port %>

CMD [ "node", "<%= props.script_path %>" ]
