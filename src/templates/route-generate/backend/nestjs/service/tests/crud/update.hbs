describe('Update', () => {
	it('should return an updated {{{props.model_name_Lcfirst}}}', async () => {
		const updateSpy = jest.spyOn(prisma.{{{props.model_name_Lcfirst}}}, 'update');
		expect(service.update({{#if props.isPrimaryKeyNumber}}1{{else}}'1'{{/if}}, mockUpdate{{{props.model_name_sUcfirst}}})).resolves.toBe(mock{{{props.model_name_sUcfirst}}});
		expect(updateSpy).toHaveBeenCalledTimes(1);
		expect(updateSpy).toHaveBeenCalledWith({ where: { {{props.model_primary_key_name}}: {{#if props.isPrimaryKeyNumber}}1{{else}}'1'{{/if}} }, data: mockUpdate{{{props.model_name_sUcfirst}}} });
	});
});
