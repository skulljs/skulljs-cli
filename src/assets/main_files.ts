import { FilesToTransform } from '@src/types/commands/new'

const main_files: FilesToTransform = {
  toTransform: [
    {
      path: 'new/',
      part: '',
      filename: 'kestrel-cli.json',
      createPath: '',
      convert: false,
    },
  ],
}
export default main_files
